# .github/workflows/ci.yml
#
# GitHub Actions workflow for Jebao MQTT Bridge
# - Runs tests on every push/PR
# - Optionally deploys to Pi on main branch

name: CI/CD

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest flake8
          
      - name: Lint with flake8
        run: |
          # Stop on syntax errors
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          # Warnings
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=100 --statistics
          
      - name: Check syntax
        run: |
          python -m py_compile jebao_mqtt_bridge.py

  # Optional: Deploy to Pi when pushing to main
  # Uncomment and configure secrets to enable
  #
  # deploy:
  #   needs: test
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   
  #   steps:
  #     - name: Deploy to Raspberry Pi
  #       uses: appleboy/ssh-action@v1.0.0
  #       with:
  #         host: ${{ secrets.PI_HOST }}
  #         username: ${{ secrets.PI_USER }}
  #         key: ${{ secrets.PI_SSH_KEY }}
  #         script: |
  #           cd ~/jebao-mqtt
  #           git pull origin main
  #           pip3 install -r requirements.txt
  #           sudo systemctl restart jebao-mqtt
